### 可用性

| 項番 | 項目 | 要件 | 根拠 |
|---|---|---|---|
| NFR-001 | 運用時間 | 24時間365日(オンラインECのため営業時間の制約はない) | 業務フロー図(To-Be)で電話注文(営業時間内のみ)からの脱却を課題としているため([[01_business_flow]]参照) |
| NFR-002 | 決済機能停止時の扱い | `STRIPE_SECRET_KEY`未設定時は決済機能を無効化し、「Stripeが設定されていません」を表示して他機能(カート閲覧等)は継続提供する | `backend/app/main.py` の `/payment/checkout`, `/payment/complete` の実装(Stripe未設定を個別にチェックしエラーを返す作り)に基づく |

### 性能・拡張性

| 項番 | 項目 | 要件 | 根拠 |
|---|---|---|---|
| NFR-003 | レスポンスタイム | カート操作・注文金額計算のAPI応答は3秒以内 | 顧客の購入離脱防止 |
| NFR-004 | 決済セッション作成のタイムアウト | Stripe Checkout Session作成(`/payment/checkout`)は10秒以内に完了しない場合エラー表示とする | 外部API(Stripe)呼び出しを含む処理のため |

### 運用・保守性

| 項番 | 項目 | 要件 | 根拠 |
|---|---|---|---|
| NFR-005 | 決済関連のログ出力 | 決済セッション作成・決済完了処理の成功/失敗をログに記録する | 決済は金銭が絡むため、障害・不正調査のための追跡可能性が必要 |
| NFR-017 | 返金関連のログ出力(2026-07-11追加) | 注文キャンセル(F-031)・返品承認(F-033)によるStripe返金処理の成功/失敗をログに記録する | NFR-005と同様、返金も金銭が絡むため追跡可能性が必要。[[../requirements/use_cases/UC-006\|UC-006]], [[../requirements/use_cases/UC-008\|UC-008]] |
| NFR-006 | クーポン使用状況の監視 | クーポンの`used_count`が`max_uses`に近づいた場合を運用側で把握できるようにする | クーポン不正利用対策(業務フロー図の課題点)に対応するため |
| NFR-018 | 自動テストによる回帰防止(2026-07-12追加) | backendの主要フロー(注文の状態遷移・在庫/クーポンのロールバック・認証/認可)を`pytest`による結合テストで検証し、`pytest-cov`でカバレッジ70%以上を維持する。DBはテスト専用インスタンスを使用し、Stripe・SMTP等の外部サービス呼び出しはモック化して実際の課金・送金・送信を発生させない | 機能追加のたびに手動でのcurl確認に頼っていたため、既存挙動の回帰を機械的に検知できるようにする。単体テスト(サービス層分離)は現状のモジュール構成(エンドポイント関数に直接DBアクセスを書く構成)では困難なため対象外とし、将来レイヤ分離する際の前提として位置付ける |
| NFR-019 | frontendの自動テストによる回帰防止(2026-07-12追加) | 画面単位のコンポーネントテストを`Vitest`+`React Testing Library`で実装し、`api/*.js`の各関数は`vi.mock`でモック化してAPI応答パターン(成功・失敗)ごとの表示・操作を検証する。加えて、登録/ログイン・購入・お気に入り・管理者操作のゴールデンパスは`Playwright`によるE2Eテストで検証し、隔離用の一時SQLite DBを用いた実バックエンドに対して実行する(Stripe・SMTPは無効化) | backend側(NFR-018)と同様、画面側の回帰も機械的に検知できるようにするため。E2Eはコンポーネントテストでは検知できない画面間遷移・実API連携の不具合を補う目的で導入する |
| NFR-020 | CIによるテスト自動実行(2026-07-12追加) | `main`へのpush・PR作成/更新時に、GitHub Actions(`.github/workflows/ci.yml`)でbackend(`pytest`、カバレッジ70%ゲート込み)とfrontend(`vitest`、カバレッジゲート込み+`npm run build`)を自動実行する。Playwright E2Eはサーバー起動を伴い実行時間・安定性のコストが大きいため、現時点ではCI対象外とし手元実行のみとする | NFR-018・NFR-019で整備したテストが手動実行に依存すると形骸化するため、push・PRのたびに機械的に実行されることを保証する |
| NFR-021 | frontendカバレッジ閾値ゲート(2026-07-12追加) | `vitest`のカバレッジ閾値(statements 65%・branches 55%・functions 55%・lines 70%)を`vite.config.js`に設定し、下回った場合はテスト失敗(CIも失敗)とする。`api/*.js`(fetchの薄いラッパーで各テストでは`vi.mock`により中身が実行されない)・`App.jsx`/`MainView.jsx`(コンポーネントの配線のみで実質的なロジックがない)・`AuthContext.jsx`(未テスト、今後の課題)はカバレッジ計測対象から除外し、実質的にテスト対象としているcomponents/pagesの実測値を基準に閾値を設定した | NFR-019導入時は「まず主要画面をカバーする第一段階」としてゲートを設けていなかったが、閾値なしでは将来的にテストが形骸化するリスクがあるため、backend(NFR-018)と同様にゲート化した |

### 移行性

現時点で対象外(既存システムからのデータ移行は発生しないため)。

### セキュリティ

| 項番 | 項目 | 要件 | 根拠 |
|---|---|---|---|
| NFR-007 | 認証方式 | JWT(HS256)によるトークン認証。有効期限60分 | `backend/app/auth.py` の実装(`ALGORITHM = "HS256"`, `ACCESS_TOKEN_EXPIRE_MINUTES = 60`)に基づく |
| NFR-008 | 決済情報の非保持方針 | クレジットカード番号等の決済情報は自システムで保持せず、Stripeに委任する | `backend/app/main.py` の実装(カード情報を扱わずStripe Checkout Sessionへリダイレクトする構成)に基づく |
| NFR-009 | 決済完了時の権限チェック | 決済完了処理(`/payment/complete`)では、Stripeセッションの`user_id`と現在のログインユーザーが一致することを確認する | UC-002 例外フロー9aに対応。他ユーザーの決済セッションの不正利用を防止するため |
| NFR-010 | 通信の暗号化 | 全通信をHTTPSで行う | 認証情報・決済関連情報を扱うため |
| NFR-011 | 管理者権限チェック | 管理者向けエンドポイント(`/admin/*`)は、`is_admin=true`のユーザーのみアクセス可能とする。一般顧客がアクセスした場合は403エラーとする | `backend/app/auth.py`の`get_current_admin`実装(`is_admin`チェック、403「管理者権限が必要です」)に基づく |
| NFR-022 | ログイン・会員登録へのレート制限(2026-07-13追加) | `POST /auth/login`は同一IPアドレスから15分間に10回、`POST /auth/register`は同一IPアドレスから1時間に5回を超えるリクエストがあった場合、429エラーとする | ブルートフォース攻撃(パスワード総当たり)・大量アカウント登録(スパム)対策。一般的な業界標準との比較監査の過程で、認証系エンドポイントにレート制限が存在しないギャップとして識別された |

- NFR-022の実装はプロセス内メモリ上のカウンタ(`backend/app/rate_limit.py`)によるものであり、外部ストア(Redis等)を用いない。そのため、複数プロセス/複数インスタンスでの水平スケール構成では各プロセスが独立したカウンタを持ち、実質的な制限値が意図した値の「プロセス数倍」になる制約がある。本プロジェクトの規模(個人学習用ECサイト、単一プロセスでの運用を想定)では許容範囲と判断し、外部ストア導入は将来的な課題とする

### 個人情報保護方針(2026-07-11追加)

| 項番 | 項目 | 要件 | 根拠 |
|---|---|---|---|
| NFR-012 | 決済情報の非保持(NFR-008の再掲) | クレジットカード番号等の決済情報は自システムで保持せず、Stripeに委任する | NFR-008と同一。決済情報は本方針でも最重要項目のため参照する |
| NFR-013 | 保持する個人情報の範囲 | 自システムのDBが保持する個人情報は、メールアドレス・ハッシュ化済みパスワード(`User`)、配送先の氏名・郵便番号・住所・電話番号(`Address`)、レビュー投稿内容(`Review`、任意入力のコメントに個人情報が含まれる可能性がある)に限られる | `backend/app/models.py`の`User`, `Address`, `Review`モデルの実装に基づく |
| NFR-014 | パスワードの保管方式 | パスワードは平文で保存せず、ハッシュ化して保存する | `backend/app/auth.py`の実装(`hashed_password`列、ハッシュ化関数使用)に基づく |
| NFR-015 | 個人情報の利用目的 | 保持する個人情報は、注文処理・配送・カスタマーサポートの目的にのみ利用し、目的外利用(第三者への販売等)は行わない | 一般的な個人情報保護法(日本の個人情報保護法における利用目的の特定・公表の考え方)に基づく努力目標。本プロジェクトはプライバシーポリシー等の法的文書の整備までは対象外とし、開発方針としての明記に留める |
| NFR-016 | 個人情報の削除 | 退会機能(F-030, UC-005)により、顧客自身の操作でメールアドレス等の個人情報を匿名化できる。ただし物理削除ではなく論理削除+匿名化であり、注文履歴・レビューは業務記録として残る点に留意(2026-07-11: 実装により解消。従来は未実装として今後の課題としていた) | `03_function_list.md`のF-030、[[../requirements/use_cases/UC-005\|UC-005]] |

- 上記は開発者向けの内部方針の明記であり、法務レビューや実際のプライバシーポリシー(顧客向け公開文書)の作成を代替するものではない。本番運用する場合は、個人情報保護法等の法令に基づく正式なプライバシーポリシーの整備を別途行うこと

### 環境・エコロジー

現時点で対象外。

## 参照

- [[04_conceptual_er]] のCOUPONエンティティ(`max_uses`, `used_count`)
- [[01_use_cases]] UC-002(決済完了処理の権限チェック)
